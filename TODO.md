# TODO

- Implement Ansible-like discovery on the remote node to capture the current state of the target agent system before any modification.
- Ensure control .ini files have .sapply.ini postfix/extension, not just .ini.
